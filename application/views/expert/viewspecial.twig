{% extends "layout/specialhtml.twig" %}
{% block content %}
    {% include 'layout/bread-crumbs-special.twig' %}

    <div class="page-name">
        <h1>{{ opinion.title|striptags }}</h1>
    </div>

    <div class="page-content">
        <div class="text">
            <p>{{ opinion.text|raw }}</p>
        </div>
    </div>
{% endblock content %}